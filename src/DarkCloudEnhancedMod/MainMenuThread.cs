namespace DarkCloudEnhancedMod
{
    /// <summary>
    /// Legacy shared flag still observed by the remaining static feature
    /// methods while the session state machine migrates to lifecycle-managed
    /// modules.
    /// </summary>
    public class MainMenuThread
    {
        public static bool userMode = false;
    }
}
